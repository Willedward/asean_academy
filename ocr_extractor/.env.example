# Optional vision backend. Export privately or load with uv --env-file.
OPENAI_API_KEY=
OCR_MODEL=gpt-5.4-mini-2026-03-17
# PostgreSQL connection string for the trusted worker, used only with --postgres.
DATABASE_URL=
# Optional directory containing eng.traineddata and other OCR language data.
TESSDATA_PREFIX=
